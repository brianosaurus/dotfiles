# General Ack Settings
# ####################################################################
#
# only search with case sensitivity if there is mixed case
--smart-case
# follow symlinks
--follow
# set up the pager
--pager=/usr/bin/less -E

--type-set=html=hbs
